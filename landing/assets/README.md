# Landing static assets

Drop `didaflow-whitepaper.pdf` here. It is linked from `/whitepaper/`
(the "PDF version — Download" button) at the path `/assets/didaflow-whitepaper.pdf`.
Until the file is added, that link 404s — this is an intentional placeholder.
